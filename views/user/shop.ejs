<%- include('../layouts/userloggedin.ejs') %>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>

<div class="container-fluid pt-5 ">
        
    <div class="row px-xl-5">
        <!-- Shop Sidebar Start -->
        <div class="col-lg-3 col-md-12">
            <!-- Price Start -->
            <div class="border-bottom mb-4 pb-4">
                <h3 class="font-weight-semi-bold mb-4">PRICE</h3>
                    <div class="custom-control custom-checkbox d-flex align-items-center justify-content-between mb-3">
                        <!-- <input type="radio" class="custom-control-input" name="sort"  id="price-all"> -->
                        <label class="  " for="price-all">SORT BY</label>
                        <!-- <span class="badge border font-weight-normal">1000</span> -->
                    </div>
                    <div class="">
                        <input type="checkbox" class="" name='sort' value="lastest arrivals" id="price-1" onclick="filter()">
                        <label class="" for="price-1">LATEST ARRIVALS</label>
                        <!-- <span class="badge border font-weight-normal">150</span> -->
                    </div>
                    <div class="">
                        <input type="checkbox" class="" name="sort"  value="Low" id="price-2" onclick="filter()">
                        <label class="" for="price-2">LOWEST TO HIGHEST</label>
                        <!-- <span class="badge border font-weight-normal">295</span> -->
                    </div>
                    <div class="">
                        <input type="checkbox" class="" name="sort" value="High" id="price-3" onclick="filter()">
                        <label class="" for="price-3">HIGHEST TO LOWEST</label>
                        <!-- <span class="badge border font-weight-normal">246</span> -->
                    </div>
                 
            </div>
            <!-- Price End -->
            
            <!-- Color Start -->
            <div class="border-bottom mb-4 pb-4">
                <h3 class="font-weight-semi-bold mb-4">CATEGORY</h3>
                    <!-- <div class="custom-control custom-checkbox d-flex align-items-center justify-content-between mb-3"> -->
                        <!-- <input type="checkbox" class="custom-control-input"  id=""> -->
                        <!-- <label class="custom-control-label" for="price-all">All Category</label> -->
                        <!-- <span class="badge border font-weight-normal">1000</span> -->
                    <!-- </div> -->
                    <%for(i=0;i<category.length;i++){%>
                    <div class=" ">
                        <input type="checkbox" class="" name="category" value="<%=category[i].name%>" onclick="filter()">
                        <label class="" for="color-1"><%=category[i].name%></label>
                        <!-- <span class="badge border font-weight-normal">150</span> -->
                    </div>
                    <%}%>

                    <!-- <div class="custom-control custom-checkbox d-flex align-items-center justify-content-between mb-3"> -->
                        <!-- <input type="checkbox" class="custom-control-input" id="color-2"> -->
                        <!-- <label class="custom-control-label" for="color-2">Women</label> -->
                        <!-- <span class="badge border font-weight-normal">295</span> -->
                    <!-- </div> -->
                    <!-- <div class="custom-control custom-checkbox d-flex align-items-center justify-content-between mb-3"> -->
                        <!-- <input type="checkbox" class="custom-control-input" id="color-3"> -->
                        <!-- <label class="custom-control-label" for="color-3">Kids</label> -->
                        <!-- <span class="badge border font-weight-normal">246</span> -->
                    <!-- </div> -->
                   
                   
            </div>

            <!-- Color End -->

            <!-- Size Start -->
            
            <!-- Size End -->
        </div>
        <!-- Shop Sidebar End -->


        <!-- Shop Product Start -->
        <div class="col-lg-9 col-md-12" >
            <div class="row  pb-3"> 
                <div id="products" class="row pb-3">
                <% if(products){ %>
                    <% for (let i=0;i< products.length;i++){ %>
                <a href="/viewproduct?id=<%= products[i].id %>" class="text-dark text-decoration-none"><div class="col-lg-4 col-md-6 col-sm-12 pb-1">
                    <div class="card product-item border-gray mb-4" style="background-color:#EDf1ff;">
                        
                <div class="card-header product-img position-relative overflow-hidden bg-transparent border p-0">
                    <% if(products[i].image){ %>
                    <img class="img-fluid w-100" src="/admin/assets/uploads/<%= products[i].image[1] %>" alt="">
                    <% }%>
                   
                </div>
                <div class="card-body border-left border-right text-center p-0 pt-4 pb-3">
                  <h6 class="text-truncate mb-3"><%= products[i].name %></h6>
                    <div class="    ">
                        <h6 >₹<%= products[i].price %></h6><h6 class="text-muted ml-2"></h6> 
                        <!-- <% if(user){%>
                        <a onclick="addtowhislist('<%=products[i]._id%>')">
                            <button class="buy-btn btn text text-white"><i class="fa  fa-heart"></i></button> 
                            <%} else {%>
                                <a onclick="wishlistlogin('login')">
                                    <button class="buy-btn btn text text-white"><i class="fa  fa-heart"></i></button> 
                                    <%}%>

                        </a> -->
                    </div>
                </div>
                <div class="card-footer d-flex justify-content-between bg-light border" >
                    <% if(user){%>
                    <a onclick="addtowhislist('<%=products[i]._id%>')" class="btn btn-sm  p-0" style="font-weight:bolder;"><i class="fas fa-heart mr-2"  style="color:red"></i>Wishlist</a>
                    <a class="btn btn-sm  p-0"  onclick="addtocart('<%=products[i]._id%>')" style="font-weight:bolder;"><i class="fas fa-shopping-cart mr-2 text-dark " ></i>Add To Cart</a>
                     <%} else {%>
                        <a  onclick="login('message')" class="btn btn-sm  p-0" style="font-weight:bolder;"><i class="fas fa-heart  mr-2" style="color:red"></i>Wishlist</a>
                        <a class="btn btn-sm  p-0 "  onclick="login('message')" style="font-weight:bolder;"><i class="fas fa-shopping-cart text-dark mr-2" ></i>Add To Cart</a>
                        <%}%>
                </div>
                    </div>
                </div></a>
                <%}} else{%>
                    <div class="flex-c-m flex-w w-full p-t-45">
                       <span>NO product</span>
                    </div>
                   
                   <% } %>
                </div>
                <div class="col-12 pb-1">
                    <nav aria-label="Page navigation">
                      <ul class="pagination justify-content-center mb-3">
                        <li class="page-item disabled">
                          <a class="page-link" href="#" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                          </a>
                        </li>
                        <li class="page-item active"><a class="page-link" href="#">1</a></li>
                        <li class="page-item"><a class="page-link" href="#">2</a></li>
                        <li class="page-item"><a class="page-link" href="#">3</a></li>
                        <li class="page-item">
                          <a class="page-link" href="#" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                          </a>
                        </li>
                      </ul>
                    </nav>
                </div>
            </div>
        </div>
        <!-- Shop Product End -->
    </div>
</div>

<script>
    function addtocart(id){
      const value=id
      console.log(value);
      $.ajax({
        url:'/addtocart',
        type:'POST',
        data:{cartid:value},
        success:function(response){
         console.log(response);
         Swal.fire({
        icon: 'success',
        title: 'Added to Cart',
        showConfirmButton: false,
        timer: 800
        })
        },
        error:function(response){
            //    alert('Error adding Product to cart')
        }
      })
    }

    function login(id){
        Swal.fire({
  icon: 'error',
  title: 'PLEASE LOGIN!',
  showConfirmButton: false,
  timer: 900
}) 
     
    }

    function addtowhislist(id){
        const value=id
        console.log(value);
        $.ajax({
            url:'/addtowishlist',
            type:'POST',
            data:{wishlistid:value},
            success:function(response){
                Swal.fire({
                icon: 'success',
                title: 'Added to Wishlist',
                showConfirmButton: false,
                timer: 800
        })
            },
            error:function(response){

            }
        })
    }


</script>

<script>
 function filter(){
    const checkbox=document.getElementsByName('category');
    const price=document.getElementsByName('sort')
    const checkboxvalue=[];
    let pricevalue
    for(i=0; i<checkbox.length;i++){
        console.log(checkbox.length);
      if(checkbox[i].checked){
        checkboxvalue.push(checkbox[i].value)
      }else{
        const index=checkboxvalue.indexOf(checkbox[i].value);
        if(index!==-1){
            checkboxvalue.splice(index,1)
        }
      }
    }
    console.log(checkboxvalue);
    for(i=0;i<price.length;i++){
        if(price[i].checked){
          pricevalue=price[i].value
        }
    }
    console.log(pricevalue);
    
    $.ajax({
        url:'/updateshop',
        type:'post',
        data:{category:checkboxvalue,sortedvalue:pricevalue},
        success:function(data){
            console.log(data);
            let products;
            products=data.products
            console.log(products);
             document.getElementById('products').innerHTML=`
             ${products ? 
             products.map(product => 
      `<a href="/viewproduct?id=${product.id}" class="text-dark text-decoration-none">
        <div class="col-lg-4 col-md-6 col-sm-12 pb-1">
          <div class="card product-item border-gray mb-4" style="background-color:#EDf1ff;">
            <div class="card-header product-img position-relative overflow-hidden bg-transparent border p-0">
              ${product.image ? `<img class="img-fluid w-100" src="/admin/assets/uploads/${product.image[1]}" alt="">` : ''}
            </div>
            <div class="card-body border-left border-right text-center p-0 pt-4 pb-3">
              <h6 class="text-truncate mb-3">${product.name}</h6>
              <div>
                <h6>₹${product.price}</h6><h6 class="text-muted ml-2"></h6> 
                ${data.products.user ? 
                  `<a onclick="addtowhislist('${product._id}')">
                  </a>` :
                  `<a onclick="wishlistlogin('login')">
                  </a>`}
              </div>
            </div>
            <div class="card-footer d-flex justify-content-between bg-light border">
              ${data.products.user ? 
                `<a onclick="addtowhislist('${product._id}')" class="btn btn-sm p-0" style="font-weight:bolder;">
                  <i class="fas fa-heart mr-2" style="color:red"></i>Wishlist
                </a>
                <a class="btn btn-sm p-0" onclick="addtocart('${product._id}')" style="font-weight:bolder;">
                  <i class="fas fa-shopping-cart mr-2 text-dark"></i>Add To Cart
                </a>` :
                `<a onclick="login('message')" class="btn btn-sm p-0" style="font-weight:bolder;">
                  <i class="fas fa-heart mr-2" style="color:red"></i>Wishlist
                </a>
                <a class="btn btn-sm p-0" onclick="login('message')" style="font-weight:bolder;">
                  <i class="fas fa-shopping-cart text-dark mr-2"></i>Add To Cart
                </a>`}
            </div>
          </div>
        </div>
      </a>`).join('') : `
    <div class="flex-c-m flex-w w-full p-t-45">
        <span>NO product</span>
    </div>
`}`;
            

        }
    })


 }

</script>
<%-include('../layouts/userFooter.ejs') %>



